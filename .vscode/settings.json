{
  // Zephyr 配置
  "zephyr.base": "${workspaceFolder}/nxp-zsdk/zephyr",
  "zephyr.sdk": "",

  // Python 配置 (使用相对路径，VSCode 会相对于 workspaceFolder 解析)
  "python.defaultInterpreterPath": ".venv/bin/python",
  "python.terminal.activateEnvironment": false,

  // CMake 配置
  "cmake.configureOnOpen": false,

  // C/C++ 配置 (ARM GCC 路径由 setup_env.sh 设置)
  // 如果系统已安装 ARM GCC，可取消注释并修改路径：
  // "C_Cpp.default.compilerPath": "/usr/local/arm-none-eabi/bin/arm-none-eabi-gcc",

  // 文件排除
  "files.exclude": {
    "**/.git": true,
    "**/.venv": true,
    "**/build": true,
    "**/__pycache__": true
  },

  // 终端工作目录 (VSCode 会先切换到此目录再启动 shell)
  "terminal.integrated.cwd": "${workspaceFolder}",

  // macOS 终端配置
  // --init-file 让 bash 启动时执行 setup_env.sh
  "terminal.integrated.profiles.osx": {
    "Zephyr Bash": {
      "path": "/bin/bash",
      "args": ["--init-file", "setup_env.sh"]
    },
    "bash": {
      "path": "/bin/bash"
    }
  },
  "terminal.integrated.defaultProfile.osx": "Zephyr Bash",

  // Linux 终端配置
  "terminal.integrated.profiles.linux": {
    "Zephyr Bash": {
      "path": "/bin/bash",
      "args": ["--init-file", "setup_env.sh"]
    },
    "bash": {
      "path": "/bin/bash"
    }
  },
  "terminal.integrated.defaultProfile.linux": "Zephyr Bash",

  // Windows 终端配置 (Git Bash)
  "terminal.integrated.profiles.windows": {
    "Git Bash": {
      "path": "C:\\Program Files\\Git\\bin\\bash.exe",
      "args": ["--init-file", "setup_env.sh"]
    },
    "PowerShell": {
      "source": "PowerShell"
    }
  },
  "terminal.integrated.defaultProfile.windows": "Git Bash"
}
